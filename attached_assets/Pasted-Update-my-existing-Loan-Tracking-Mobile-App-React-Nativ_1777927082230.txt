Update my existing Loan Tracking Mobile App (React Native + Firebase) with the following UI fixes and enhancements:

---

🛠️ 1. Fix "Total Loan Disbursed" Overflow Issue

- Currently the "Total Loan Disbursed" card is going outside the screen
- Fix layout so it stays inside screen width

Apply:

card: {
  width: "100%",
  marginHorizontal: 0,
  padding: 16,
  borderRadius: 12
}

- Wrap all dashboard content inside:

container: {
  flex: 1,
  padding: 16
}

- Avoid using fixed widths like "width: 400"

---

📱 2. Fix Top Gap in Borrower Dashboard

- There is unwanted space at top of screen

Fix:

- Use "SafeAreaView"
- Remove any "marginTop: 30+"
- Add:

import { SafeAreaView } from "react-native-safe-area-context";

<SafeAreaView style={{ flex: 1 }}>

- Also control status bar:

import { StatusBar } from "expo-status-bar";
<StatusBar style="dark" />

---

🎨 3. Fix Black Box Issue in Login Page

- Username & Password fields showing black background

Fix:

- Set proper input styles:

input: {
  backgroundColor: "#fff",
  borderRadius: 10,
  padding: 12,
  marginBottom: 12,
  borderWidth: 1,
  borderColor: "#ddd",
  color: "#000"
}

- Also ensure:

placeholderTextColor="#999"

- Remove any:

backgroundColor: "black"

---

✨ 4. Improve Login UI (Modern Look)

- Center the form
- Add card-style login box

loginContainer: {
  flex: 1,
  justifyContent: "center",
  padding: 20,
  backgroundColor: "#f5f7fb"
}

loginCard: {
  backgroundColor: "#fff",
  padding: 20,
  borderRadius: 15,
  elevation: 5
}

- Add title:

fontSize: 22,
fontWeight: "bold",
textAlign: "center"

---

💎 5. Improve Dashboard UI (Admin & Borrower)

- Convert all sections into cards
- Add shadow & spacing:

card: {
  backgroundColor: "#fff",
  padding: 16,
  borderRadius: 15,
  marginBottom: 12,
  elevation: 4
}

- Use proper alignment:

flexDirection: "row",
justifyContent: "space-between"

---

📊 6. Fix Layout Responsiveness

- Ensure no content goes outside screen
- Use:

width: "100%"
flex: 1
flexWrap: "wrap"

- Avoid:
  ❌ absolute positioning (unless required)
  ❌ fixed pixel widths

---

🎯 7. Add Header UI (Premium Look)

- Add gradient header using Expo:

import { LinearGradient } from 'expo-linear-gradient';

<LinearGradient
  colors={['#4facfe', '#00f2fe']}
  style={{
    padding: 20,
    borderBottomLeftRadius: 20,
    borderBottomRightRadius: 20
  }}
>

---

🎨 8. Improve Overall Theme

- Use consistent colors:
  
  - Primary: "#4facfe"
  - Background: "#f5f7fb"
  - Card: "#ffffff"

- Add icons using:

@expo/vector-icons

---

🧹 9. General Cleanup

- Remove:
  - Extra margins
  - Nested unnecessary Views
  - Inline styles (move to StyleSheet)

---

📦 10. Expected Output

- Fully fixed UI (no overflow, no gaps)
- Clean login screen (no black boxes)
- Modern dashboard design
- Responsive layout across devices
- Smooth user experience

---

Ensure:
✔ No UI breaks
✔ No overlapping elements
✔ Clean spacing everywhere
✔ Works on all screen sizes

---